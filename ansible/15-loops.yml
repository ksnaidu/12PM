---
- name: loops
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello, {{ item }}" # Item here is reserved keyword.
    loop:
    - suresh
    - ramesh
    - karthik
    